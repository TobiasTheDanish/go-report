package view

templ card() {
	<div class="flex flex-col border rounded-xl shadow w-fit">
		{ children... }
	</div>
}

templ cardHeader() {
	<div class="p-6 w-full">
		{ children... }
	</div>
}

templ cardContent() {
	<div class="w-full p-6 pt-0">
		{ children... }
	</div>
}

templ cardFooter() {
	<div class="w-full p-6 pt-0">
		{ children... }
	</div>
}

templ githubIcon() {
	<img src="assets/svg/github-mark.svg" alt="GitHub logo" class="w-[28px]"/>
}
